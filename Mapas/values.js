let mapa = new Map();
mapa.set('chave1', 'valor1');
mapa.set('chave2', 'valor2');

let iterator = mapa.values();
for (let valor of iterator) {
    for (let valor of iterator) {
        console.log(valor);
    }
}